include(CMakeFindDependencyMacro)

# 导入目标
include(${CMAKE_CURRENT_LIST_DIR}/rs_readerTargets.cmake)

# 设置包含目录
set(rs_reader_INCLUDE_DIRS "${CMAKE_INSTALL_PREFIX}/include/rs_reader")

# 设置库目标
set(rs_reader_LIBRARIES rs_reader::rs_reader)